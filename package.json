{
  "name": "lint-kit-monorepo",
  "private": true,
  "packageManager": "pnpm@10.18.1",
  "scripts": {
    "prepare": "husky",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.3.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.1",
    "prettier": "^3.6.2"
  }
}